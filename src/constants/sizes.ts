const iconSizes = {
  basicSize: 30,
  middleSize: 25,
  smallSize: 20,
};

const fontSizes = {
  basicSize: 17,
  largeSize: 33,
  middleSize: 15,
  smallSize: 12,
};

const radiusSizes = {
  smallRadius: 10,
  basicRadius: 20,
  largeRadius: 40,
};

export { iconSizes, radiusSizes, fontSizes };

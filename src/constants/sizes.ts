const iconSizes = {
  basicSize: 30,
  middleSize: 25,
  smallSize: 20,
};

const inputSizes = {
  maxLength: 50,
};

const radiusSizes = {
  smallRadius: 10,
  basicRadius: 20,
  largeRadius: 40,
};

export { iconSizes, inputSizes, radiusSizes };
